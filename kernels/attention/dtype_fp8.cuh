#pragma once

// Placeholder for FP8 attention vector specializations. The existing aggregate
// attention_dtypes.h includes this header, so keep the include resolvable even
// before FP8 paged-attention kernels are implemented.
